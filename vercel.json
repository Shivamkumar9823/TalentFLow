// vercel.json

{
  "rewrites": [
    { 
      // Source: Match all incoming paths (except static files)
      "source": "/(.*)", 
      
      // Destination: Rewrite the path to the root HTML file
      "destination": "/index.html" 
    }
  ]
}